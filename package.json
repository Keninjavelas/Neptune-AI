{
  "name": "neptune-ai",
  "version": "1.0.0",
  "description": "Neptune-AI — Smart Water Leak Detection & Automatic Response System",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:infra": "docker compose up",
    "start": "docker compose up"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
